config {
    testsDir "tests"
    workDir  ".nf-test"
    configFile "nextflow.config"
}

// Constrain resources for test environments (GitHub Actions has 4 cores)
process {
    withLabel: 'process_high' {
        cpus   = 2
        memory = 4.GB
    }
}
